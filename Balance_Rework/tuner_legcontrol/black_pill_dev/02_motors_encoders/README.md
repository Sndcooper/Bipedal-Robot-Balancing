# 02: Motors and encoders

With wheels lifted, use `M left right` where each value is -255..255. Confirm
that positive command means the intended forward direction, then roll each wheel
forward by hand and confirm its encoder count increases. If not, correct wiring
or the documented sign before touching balance gains.
